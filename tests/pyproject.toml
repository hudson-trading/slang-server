[project]
name = "slang-server-tests"
version = "0.1.0"
description = "Tests for slang-server"
requires-python = ">=3.9"
dependencies = [
    "pygls>=1.3.1",
    "pytest>=8.3.4",
    "pytest-asyncio>=0.25.3",
]

[tool.pytest.ini_options]
asyncio_default_fixture_loop_scope = "function"
